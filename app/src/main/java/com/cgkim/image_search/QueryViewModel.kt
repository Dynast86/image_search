package com.cgkim.image_search

import androidx.lifecycle.ViewModel

class QueryViewModel(private val queryRepository: QueryRepository) : ViewModel() {

    fun makeQuery(text: String) {
//        view
    }
}